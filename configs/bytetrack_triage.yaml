# ByteTrack configuration for triage.
# Overrides Ultralytics' packaged bytetrack.yaml defaults.

tracker:
  track_high_thresh: 0.5
  track_low_thresh: 0.1
  new_track_thresh: 0.6
  track_buffer: 30
  match_iou_threshold: 0.5
  max_age: 30
  min_hits: 3
  object_threshold: 0.10
